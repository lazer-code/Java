public class Exercises
{
    private static Queue<Integer> ;

    public static Queue<Integer> remDups (Queue<Integer> queue)
    {
        Queue<Integer> temp = new Queue<Integer>();

        while (!queue.isEmpty())
        {
            while (temp)
        }

    }
}
